<?php
$input = explode(" ", trim(fgets(STDIN)));

$sortedArr = $input;
sort($sortedArr);

$table = [];

for ($i = 0; $i < count($input); $i++){
    $currentNum = $input[$i];

    for($k = $i; $k < count($input); $k++){

    }
}

//2complex4me